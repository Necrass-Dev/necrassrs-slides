---
제출자: 김현서
세션 유형: 발표
트랙: 개발 언어 및 툴체인(Programming Languages & Tooling)
언어: 한국어
소요 시간: 00:30
---

# GraphQL은 SDL입니다: 코드젠 기반 GraphQL 프레임워크, NecrassRs 제작기

## 발표 전체 요약

 이 발표에서는 GraphQL API의 공개 계약을 Rust 코드에서 추론하기보다 GraphQL SDL로 먼저 정의하고, 그 계약에서 Rust 코드를 생성하는 방식을 소개합니다.
 이 아이디어를 적용해 만든 Rust용 GraphQL 프레임워크 NecrassRs를 시연하고, SDL 기반 코드 생성이 실제 구현에서 어떤 이점과 한계를 만드는지 살펴봅니다.

## 발표 개요

### 1. GraphQL은 SDL입니다. — 5분

- Rust용 GraphQL 프레임워크인 async-graphql을 사용하면서, Rust 코드가 GraphQL 계약의 원본이 되는 방식에 계속 불편함을 느낌

- GraphQL은 프론트엔드가 필요한 데이터를 직접 표현할 수 있다는 점에서 강력한 수단
- 강점을 살리려면 프론트엔드와 백엔드가 공개 API의 구조와 제약을 함께 읽고 이해할 수 있어야 함 → 이 계약을 특정 호스트 언어와 독립된 형태로 표현할 필요가 있음.
- 계약을 호스트 언어가 아닌 GraphQL SDL로 직접 표현하면 어떨까? → `.graphql`을 계약의 원본으로 사용
- 이 계약에서 Rust 계약과 연결 코드를 생성하고, 사용자는 구현을 채움 → NecrassRs의 시작
- Tonic의 코드 생성 방식, Relay의 선언적 데이터 요구 표현에서 영감을 받음

### 2. NecrassRs의 원리 — 12분

#### 빌드 시점: SDL → Rust 계약

- `cargo build` → `necrassrs-build` 호출
- Apollo Compiler로 SDL 파싱·스키마 검증 → 생성기가 지원하는 타입과 이름 확인
- SDL의 객체·필드 정의 → 인자 구조체, 리졸버 trait, dispatch 코드 생성
- dispatch: GraphQL 필드를 Rust 리졸버 메서드 호출에 연결
- 재생성 가능한 계약·연결 코드는 `OUT_DIR`에 저장, 실행에 사용할 SDL도 포함 → 생성기가 소유하는 코드

#### 사용자 코드: 선언 동기화와 업무 로직

- 사용자는 `src/resolvers.rs`의 리졸버 메서드에 로직 작성 → 사용자가 소유하는 코드

- 단순히 코드를 매번 덮어쓰는 생성기가 아니라, SDL 변경에 따라 함수 선언은 갱신하면서 기존 사용자 구현은 보존하는 구조
- 빌드 시 SDL에 맞춰 리졸버 선언 동기화
- 유지되는 메서드의 본문은 보존, 새 필드에는 `unimplemented!()` 골격 추가
- 삭제된 필드의 메서드는 제거

#### 실행 시점: Rust 타입 검사 이후의 일

- HTTP handler에서 요청과 Context 준비. 현재 어댑터·초기화 템플릿은 Axum 기반
- Apollo 쪽의 요청 검증과 변수 coercion은 재사용하고, 필드 수집·인자 처리·resolver dispatch·결과 조립은 NecrassRs가 담당
- Rust 코드의 컴파일 성공 ≠ GraphQL 실행 명세 준수. 입력 처리·오류 전파는 별도 구현과 테스트 필요
- 실행 코어는 Axum과 분리

### 3. SDL에서 실행까지 — 데모(프로젝트 캡쳐 및 컴파일된 결과 시연) 3분

1. `necrass init`으로 생성된 `examples/basic-server`의 프로젝트 구조 확인
2. schema.graphql → 자동 생성된 계약 코드 → 사용자가 구현한 resolver 코드의 대응 관계 확인
3. GET `/graphql`의 GraphiQL에서 스키마 탐색으로 쿼리 시연
4. greeting MVP 예제의 성공 응답과 `USER_NOT_FOUND` 오류 비교

### 4. 얻은 것 - 2분

- SDL에서 Rust 계약·연결 코드 생성 → 반복되는 수동 선언 감소
- 스키마와 구현의 타입 불일치를 컴파일 단계에서 발견 가능
- 코드 생성으로 보장할 수 있는 영역과 GraphQL 런타임에서 검증해야 하는 영역의 경계를 확인함

### 5. 남은 과제 및 한계 - 2분

- 현재는 MVP 단계로 전체 GraphQL 명세를 지원하지 않으며, 지원 타입 확장과 실행 규칙 검증이 더 필요함
- 필드별 리졸버 메서드가 늘어나면 파일이 길어짐 → 사용자 코드 모듈화와 선언 동기화를 함께 지원하는 것이 과제
- Apollo Compiler 1.33.0의 검증만으로는 NecrassRs가 요구하는 모든 실행 규칙을 보장할 수 없는 사례가 있어 추가 검증이 필요함
- 필드 이름 변경은 현재 삭제와 신규 생성으로 처리되기 때문에 기존 리졸버 구현을 자동으로 이전하지 못함

## 마무리 - 1분

> GraphQL API의 계약을 GraphQL SDL로 먼저 정의하고, 호스트 언어가 그 계약을 구현하도록 만들 수 있습니다.
> 이것이 “GraphQL은 SDL입니다”라는 제목으로 말하고 싶었던 의미입니다.

## 발표를 통해 청중이 얻을 수 있는 것

- SDL을 API 계약의 출발점으로 삼았을 때의 이점과 제약
- Cargo 빌드에 코드 생성을 통합하고 생성 계약과 사용자 구현을 연결하는 방법
- Rust의 컴파일 타임 타입 검사와 GraphQL 런타임 검증의 책임 차이
- 기존 파서·검증기를 재사용할 부분과 직접 구현할 실행 경계를 나눈 경험
